/**
5.1.3 산술(arithmatic) 연산자
수학 연산을 수행한다.
*/
import Foundation

func exec_5102() {
	
	let a = 4
	let b = 2
	
	print("\(a) + \(b) = \(a + b)")
	print("\(a) - \(b) = \(a - b)")
	print("\(a) * \(b) = \(a * b)")
	print("\(a) / \(b) = \(a / b)")
	print("\(a) % \(b) = \(a % b)")
}
