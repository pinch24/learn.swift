import Foundation

let a: Int = 4
print(a)

let b: Double = Double(a)
print(b)

/**
 * ### 5.1.1 할당(assign) 연산자
 * 값을 할당할 때 사용하는 연산자이다. 타입이 다른 데이터를 할당하면 오류가 발생한다.
 * 타입이 다른 데이터를 할당하려면 타입 변환(casting)을 해야한다.
 */
