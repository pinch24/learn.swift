import Foundation

func exec_5122() {
	
	let a = 4
	let b = 3
	print("\(a) % \(b) = \(a % b)")
	
	let n: Double = 5.0
	let r: Double = n.truncatingRemainder(dividingBy: 1.5)
	print(r)
}

/**
### 5.1.2 산술(arithmatic) 연산자
스위프트의 나머지 연산자는 부동소수점 타입 연산도 지원한다.
*/
