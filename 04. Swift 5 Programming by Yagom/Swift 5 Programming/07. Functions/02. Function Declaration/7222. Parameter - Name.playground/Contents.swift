func sayHello(myName: String, yourName: String) -> String {
	return "Hello \(yourName)! I'm \(myName)."
}

print(sayHello(myName: "Chris", yourName: "Brad"))

/**
 * #### 7-3 매개변수가 여러 개인 함수의 정의와 사용
 * 스위프트는 함수를 호출할 때 매개변수 이름을 표시한다.
 */
