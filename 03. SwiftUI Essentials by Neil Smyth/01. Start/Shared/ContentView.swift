import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("Hello, world!")
            .padding()
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}

/**
 # 1. SwiftUI
 SwiftUI는 선언적 구문을 사용한다.
 
 Xcode의 User Interface에 대해 배운다는 것은 내장된 컴포넌트(뷰)를 어떻게 화면에 배치하는지와 함께 애플리케이션 내에서 계산되고 처리된 데이터를 화면에 표현하는 방법을 배운다는 것이다.
 */
